# Configuration
Pour le hot reload, il faut changer les scripts de run dans la config :
```
    "beforeDevCommand": "pnpm run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "pnpm run build",
```

